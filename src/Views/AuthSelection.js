import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AuthSelection = () => {
  return (
    <View>
      <Text>AuthSelection</Text>
    </View>
  );
};

export default AuthSelection;

const styles = StyleSheet.create({});
